const db = require('../db')

describe('Employee DAO', () => {
	test.todo('Returns all instances of employee')
	test.todo('Returns the instance of employee with the given id')
})

describe('Timestamp DAO', () => {
	test.todo('Returns all timestamps in the given interval')
	test.todo('Returns all timestamps with the given id')
})
